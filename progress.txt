2026-02-03
- Decision: UI term is "Workspace". Remove "Org" concept: workspace is the organisation. Auth is local email+password + JWT.
- Imported existing portal UI from coreport-project/dev into coreport/frontend (MUI + router + login flow).
- Cleanup: renamed tenant/project/org concepts to workspace-only throughout backend + frontend.
- DB reset approved: we can drop volumes to simplify destructive schema changes.
- Hard rule: before telling Dre to check localhost:3000, run a Playwright visual sanity check (no more blind "it should work").

2026-02-04
- Task 2 (UX: Sidebar collapse) implemented: animated ease-out collapse/expand toggle, icons-only collapsed mode, persisted in localStorage.
- Task 28 (Assignments per entity): added entity_assignments table + ORM model + seed defaults (bob submitter, carol approver); verified via alembic + psql.
- Task 11 (Admin create users): added admin-only endpoint to create AuthUser + send invite email via SMTP (MailHog), plus supporting schemas.
- Task 4 (Notifications badge): added NotificationsContext + topbar badge and Playwright spec/screenshot.
- Task 6 (Logout): added avatar menu + Logout action; clears token/workspace in localStorage, notifies auth change, navigates to /login. Verified: `sudo docker compose up -d --build` (frontend Dockerfile now uses pnpm), Playwright tests passed (incl. new logout spec), screenshot saved at `artifacts/task-6-logout.png`.
- Task 5 (Notifications drawer): implemented right-side notifications drawer opened from topbar bell, with empty state + list items + mark read + mark all read (client-side optimistic stub, structured API wrappers). Verified: `sudo docker compose up -d --build`, `pnpm exec playwright test` (added `notifications-drawer.spec.ts`), screenshot saved at `artifacts/task-5-notifications-drawer.png`.
- Task 3 (Collapsed sidebar accessibility): added aria-labels for each nav item, ensured visible focus ring in collapsed icon-only mode, verified keyboard tab navigation, and added Playwright spec that collapses sidebar + checks aria-labels + tooltip hover. Verified: `sudo docker compose up -d --build`, `pnpm exec playwright test`, screenshot saved at `artifacts/task-3-sidebar-a11y.png`.
- Task 12 (Users: Workspace memberships + roles): added `workspace_memberships` table + `WorkspaceRole` enum (admin/submitter/approver/viewer). Enforced admin-only access for `POST /api/v1/users/admin-create` based on membership role. Seed now creates auth users + memberships for alice(admin), bob(submitter), carol(approver). Verified: `sudo docker compose up -d --build`, `alembic upgrade head`, `python -m app.db.seed`, `pytest`.

  Curl proof (bob is non-admin):
  ```bash
  TOKEN=$(curl -s -X POST http://localhost:8000/api/v1/auth/login \
    -H 'Content-Type: application/json' \
    -d '{"workspace_slug":"default","email":"bob@example.com","password":"dev-password"}' | jq -r .access_token)

  curl -i -X POST http://localhost:8000/api/v1/users/admin-create \
    -H "Authorization: Bearer $TOKEN" \
    -H 'Content-Type: application/json' \
    -d '{"email":"mallory@example.com","role":"viewer"}'

  # HTTP/1.1 403
  # {"detail":"Admin privileges required"}
  ```

- Task 7 (Theme: Improve dark mode styling): improved dark palette tokens + action states; added component overrides (Paper/Card borders, Drawer sidebar surface + border, ListItemButton selected states, OutlinedInput focus/hover). Updated Home page to render a Paper surface for clearer dark-mode surfaces. Verified: `sudo docker compose up -d --build`, Playwright spec `dark-mode-render.spec.ts` added + passed; screenshot saved at `artifacts/task-7-dark-mode.png`.

- Task 8 (Theme: Add System mode) (redo): added theme mode selector (Light/Dark/System) in topbar; System mode resolves via `prefers-color-scheme` and reacts to changes; preference persisted in localStorage (`coreport-theme-mode`). Verified: `sudo docker compose up -d --build`, `pnpm -C frontend exec playwright test` (added `theme-system-mode.spec.ts` proving persistence + follows Playwright media emulation); screenshot saved at `artifacts/task-8-theme-system-mode.png`.

- Task 1 (UX: Mobile-friendly shell): made layout responsive — sidebar becomes a temporary overlay drawer on mobile (opened via hamburger in topbar), content uses full-width spacing at ~360px, and topbar elements collapse appropriately. Verified: `sudo docker compose up -d --build`, `pnpm -C frontend exec playwright test` (added `mobile-shell.spec.ts`).

- Task 9 (UX: Breadcrumb component for multi-step flows): added reusable `MultiStepBreadcrumbs` component (items/current/onNavigate, click-back, responsive mobile rendering). Wired into a new `Submissions` → `New submission` multi-step wizard at `/submissions/new` as the first usage. Verified: `pnpm -C frontend exec playwright test` (all passing). Note: existing `pnpm -C frontend lint` and `pnpm -C frontend build` fail on pre-existing issues in base branch.

- Task 10 (UX: Standard multi-step page layout): added reusable `MultiStepFlowLayout` (header + breadcrumbs + step navigation region + consistent card body, responsive spacing). Applied to the submissions wizard and a new placeholder `Field mapping` wizard at `/field-mapping`. Added Playwright spec `multistep-layout.spec.ts` to assert layout renders and step breadcrumb compacts on mobile. Verified: `sudo docker compose up -d --build`, `pnpm -C frontend playwright test`.
