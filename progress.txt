2026-02-04
- Task #4: Notifications badge in topbar.
  - Added notifications state (NotificationsContext) + API stub getUnreadNotificationCount() (returns 3 for now; structured to swap to GET /notifications/unread_count later).
  - Updated Topbar notifications icon button to render MUI Badge with unread count and click handler (togglePanel placeholder).
  - Verified via Playwright screenshot: artifacts/task-4-notification-badge.png
