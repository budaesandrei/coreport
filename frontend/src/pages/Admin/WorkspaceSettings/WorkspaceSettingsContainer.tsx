import WorkspaceSettingsPresentation from './WorkspaceSettingsPresentation';

const WorkspaceSettingsContainer: React.FC = () => {
  return <WorkspaceSettingsPresentation />;
};

export default WorkspaceSettingsContainer;
